# pkm stats

Show vault statistics.

## Usage
`pkm stats [OPTIONS]`

## Options
- `--format [json|table]`: Output format (default: json).

## Description
Shows vault-level stats like total notes, dailies, tasks, orphans, unique tags, average links, and index status.

## Examples
```bash
pkm stats --format table
```
